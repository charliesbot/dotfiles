-- Extra colorscheme, installed and available via :colorscheme tokyonight.
-- Not activated: catppuccin remains the default (see catppuccin.lua).

return {
  'folke/tokyonight.nvim',
  lazy = false,
  priority = 1000,
  opts = {},
}
