-- Extra colorscheme, installed and available via :colorscheme rose-pine.
-- Not activated: catppuccin remains the default (see catppuccin.lua).

return {
  'rose-pine/neovim',
  name = 'rose-pine',
  lazy = false,
  priority = 1000,
  opts = {},
}
